require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const {data: user} = await supabase.from('profiles').select('id').eq('email', 'simonsilverboombox@gmail.com').single(); if(!user) return console.log('user not found'); console.log('USER_ID:', user.id); } run();  
